on:
  push:
    branches:
      - html

jobs:
  deploy:
    runs-on: udm

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Copy HTML to destination folder
      run: |
        cp -r ./ /home/ubuntu/docker/volumes/universodomedo/site
      working-directory: ${{ github.workspace }}
