// Etapa 11: geometria canônica compartilhada do Tutorial (editor + renderizador final). Fonte única; sem React/estado.
// Razão de altura do canvas lógico (retrato) em relação à largura — espaço de coordenadas dos Blocos (areas em %).
export const RAZAO_ALTURA_CONTEUDO_TUTORIAL = 1.4;
