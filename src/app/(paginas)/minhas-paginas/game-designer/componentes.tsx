'use client';

import { PAGINAS } from "types-nora-api";

import { ControladorSlot } from "@/layouts/ControladorSlot";

export default function PaginaGameDesignerSeres() {
    return (
        <ControladorSlot pagina={PAGINAS.minhasPaginas.gameDesigner}>
            <></>
        </ControladorSlot>
    );
};