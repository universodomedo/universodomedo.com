version: '3.8'

services:
  vite-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vite-app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    command: npm run dev
    environment:
      - NODE_ENV=development
