// STUB TEMPORÁRIO — REMOVER APÓS RESTART DO DEV SERVER.
// Só existe para o cache do webpack do dev server (preso a um módulo deletado durante o watch) conseguir compilar.
// Nada importa isto; ao reiniciar o `npm run udm`, o cache limpa e estes 2 arquivos (contexto + SPA NovoMenu) podem ser deletados.
export default function SPA__PaginaAdminGestaoNavegacao__NovoMenu() { return null; };