import { PAGINAS } from "types-nora-api";

import { ControladorSlot } from "Layouts/ControladorSlot";

export default function PaginaModeradorConquistas_Client() {
    return (
        <ControladorSlot pagina={PAGINAS.minhasPaginas.moderador.gerenciarConquistas}>
            <></>
        </ControladorSlot>
    );
};